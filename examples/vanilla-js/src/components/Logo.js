/**
 * A logo component. Renders logo.
 *
 * @type {{render: (function(): string)}}
 */
const Logo = {
    render: () => `<img alt="logo" class="logo" src="/design/images/logo.png" />`
};

export default Logo;
