import allTranslations from './translations.json';
// Getting proper translations from "locale" is not yet implemented — falling back to English ("en") for now.
const translations = allTranslations.en;

export default translations;
